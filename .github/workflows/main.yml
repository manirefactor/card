name: "ci-cd pipeline"

on: 
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js 16.x
      uses: actions/setup-node@v2
      with:
        node-version: 16.x
        cache: 'npm'
        cache-dependency-path: package-lock.json
    - name: Install dependencies
      run: npm install 
    - name: Build file
      run: npm run build
      

    - name: Deploy production to Netlify
      uses: South-Paw/action-netlify-deploy@v1.2.0
      with:
        github-token: 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
        netlify-auth-token: VWnw4TML6a1_x8Rw6DlQJ7u2EgWdl09ir2ItYPQg4b4
        netlify-site-id: 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
        build-dir: './build'
        comment-on-commit: true
